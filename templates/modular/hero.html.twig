{% set grid_size = theme_var('grid-size') %}
{% set hero_image = page.header.hero_image|first.grayscale().contrast(20).brightness(-100).colorize(0,64,0)  ? page.media[page.header.hero_image] : page.media.images|first.grayscale().contrast(20).brightness(-100).colorize(0,64,0) %}

{% include 'partials/hero.html.twig' %}
